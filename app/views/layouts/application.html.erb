<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "foundation-rails" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

  <body>
  <div class="contain to grid">
  <nav class="top-bar" data-topbar role="navigation">
      <ul class="title-area">
        <li class="name">
          <h1><a href="/"><%= image_tag("app_logo.png") %></a></h1>
        </li>
    <section class="top-bar-section">
      <ul class="right index-nav-ul">
        <% if logged_in? %>
          <li class="has-form"><%= link_to "Home",root_path, class: "button" %></li>
          <li class="has-form"><%=  link_to "Log Out",sessions_path, method: "delete", class: "button" %></li>
        <% else %>
          <li class="has-form"><%= link_to "Sign Up",new_user_path, class: "button" %></li>
          <li class="has-form"><%=  link_to "Contanct Us",new_user_path, class: "button" %></li>
        <% end %>
      </ul>
    </section>
  </nav>
</div>




    <div class="row  layout-yield">
      <%= yield %>
    </div>
  </body>
</html>
