 $(document).ready(function(){
   $(".layout-yield ").on("click",".attendee-div", function(event){
      event.preventDefault();
      alert("hey here at new file")
      // var id = $(this).attr('id')
      // var link = $(this).attr('href')
      // var data = $(this).attr("class");
      // $("div [id="+ id + "]").remove()

      // var ajaxRequest = $.ajax({
      //   url: link,
      //   method: "Post",
      //   data: data
      // })
    });
 })